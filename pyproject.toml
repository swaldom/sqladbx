[project]
name = "sqladbx"
version = "0.0.1"
description = ""
authors = [{ name = "Your Name", email = "your.email@example.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.13"
keywords = ["async", "sqlalchemy", "db", "context"]

dependencies = ["SQLAlchemy>=2.0", "starlette>=0.27"]

[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"
